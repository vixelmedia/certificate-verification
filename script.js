const DATA='certificates.json';
const norm=s=>(s||'').trim().toUpperCase();
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
async function records(){const r=await fetch(DATA,{cache:'no-store'});if(!r.ok)throw Error('Could not load records');return r.json()}
function valid(x){return `<div class="valid">✓ CERTIFICATE VERIFIED</div><div class="details"><div class="row"><div class="label">Certificate ID</div><div class="value">${esc(x.id)}</div></div><div class="row"><div class="label">Name</div><div class="value">${esc(x.name)}</div></div><div class="row"><div class="label">Programme</div><div class="value">${esc(x.programme)}</div></div><div class="row"><div class="label">Certificate Type</div><div class="value">${esc(x.certificateType)}</div></div><div class="row"><div class="label">Date Issued</div><div class="value">${esc(x.dateIssued)}</div></div><div class="row"><div class="label">Status</div><div class="value">${esc(x.status)}</div></div><div class="row"><div class="label">Issued By</div><div class="value">Vixel Media</div></div></div>`}
function invalid(id){return `<div class="invalid">✕ CERTIFICATE NOT FOUND</div><p style="margin-top:18px">The certificate ID <strong>${esc(id||'')}</strong> could not be verified. Check the ID and try again.</p>`}
async function verify(id,out){id=norm(id);if(!id){out.innerHTML=invalid('');return}try{const list=await records();const x=list.find(a=>norm(a.id)===id);out.innerHTML=x?valid(x):invalid(id)}catch(e){out.innerHTML='<p>Verification is temporarily unavailable. Please try again later.</p>';console.error(e)}}
document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('form'),input=document.getElementById('id'),out=document.getElementById('result');const id=norm(new URLSearchParams(location.search).get('id'));if(out&&id)verify(id,out);if(input&&id)input.value=id;if(form)form.addEventListener('submit',e=>{e.preventDefault();const id=norm(input.value);location.href='verify.html?id='+encodeURIComponent(id)})});
